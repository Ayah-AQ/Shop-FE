
import { AddButton } from "../../style";

const Addbutton = () => {
   return (
    <AddButton
      type="AddButton"
      class="btn btn-info mt-3 "
      style={{ marginLeft: "155px" }}
    >
      Add
    </AddButton>
  );
};

export default Addbutton;
